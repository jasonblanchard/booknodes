<div class='idea-list'>
  <div><a href='#' class='expand-sidebar-button'><<</a></div>
  <br />
  <br />
  <div class='clearfix'></div>
  <%= link_to content_tag(:div, 'All', :class => 'idea-node'), book_event_path(@event) %>

  <% @idea_list.each do |idea,number| %>
    <%= link_to content_tag(:div, raw("#{idea} <span class='info'>(#{number})</span>"), :class => 'idea-node'), idea_filter_path(@event, idea)  %>
  <% end %>
</div>
