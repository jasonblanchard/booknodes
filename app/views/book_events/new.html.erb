<div class='col-md-6'>
  <h2>New Book</h2>

  <%= form_for(@event) do |f| %>
    <div class='form-group'>
      <%= f.label :title %>
      <%= f.text_field :title, :class => 'form-control' %>
    </div>
    <%= f.submit :class => 'btn btn-default' %>
  <% end %>


</div>
